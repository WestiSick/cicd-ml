// Package main — simulator entrypoint.
//
// The simulator is a CLI invoked by the API gateway via os/exec or directly
// embedded as a library — TBD once strategies stabilise. For now it's a
// scaffold that documents the intent.
package main

import "fmt"

func main() {
	fmt.Println("simulator (scaffold)")
}
