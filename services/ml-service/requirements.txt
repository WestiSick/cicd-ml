fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
# xgboost-cpu is the CPU-only wheel — same Python API, but excludes the
# 300 MB nvidia_nccl_cu12 dependency that `xgboost` pulls in by default
# since 2.1. The slim wheel keeps the image build under control and
# avoids the spurious hash-mismatch failures we saw on slow networks
# when pip tried to download the large GPU shard in parallel.
xgboost-cpu==2.1.3
lightgbm==4.5.0
# matplotlib produces the thesis-ready PNG/PDF figures shipped via the
# `export-thesis-pack` endpoint. We use the `agg` non-interactive backend
# (set in app/export/figures.py) so the import doesn't try to talk to
# X11 in a headless container.
matplotlib==3.9.2
# Optuna powers the hyperparameter search in /train?optuna_trials=N.
# Lightweight pure-Python; tiny wheel.
optuna==4.1.0

joblib==1.4.2
python-multipart==0.0.20
httpx==0.28.1
loguru==0.7.3

# PyTorch CPU-only — used by app/models/lstm.py for the LSTM baseline.
# The CPU wheel is ~200 MB (vs ~2 GB for the CUDA variant). The thesis
# scale (≤ low thousands of rows) doesn't need GPU; CPU finishes a
# 64-hidden LSTM in seconds.
torch==2.5.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

pytest==8.3.4
ruff==0.8.4
